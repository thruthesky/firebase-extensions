import axios from "axios";
import { describe, it } from "mocha";
import { expect } from "chai";



describe("deleteAccount integration test", () => {
  it("test with invalid uid", async () => {
    /// Test is no input for the function. No need to test.
  }).timeout(10000);
});
